# Настройка автоматического перезапуска процессов

## Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

PM2 уже добавлен в `package.json` и будет установлен автоматически.

### 2. Запуск с Docker

```bash
# Пересобрать контейнер с новыми изменениями
docker-compose up -d --build

# Проверить статус
docker-compose ps

# Проверить логи
docker-compose logs -f wa-manager
```

### 3. Проверка работы PM2

```bash
# Войти в контейнер
docker exec -it wa-manager sh

# Проверить статус процессов
pm2 status

# Должны видеть 2 процесса:
# - api-server (online)
# - nextjs-server (online)
```

## Что изменилось

### ✅ Добавлено

1. **PM2** - менеджер процессов для автоматического перезапуска
2. **ecosystem.config.js** - конфигурация PM2 с настройками авто-рестарта
3. **Обновлен Dockerfile** - теперь использует PM2 для запуска процессов
4. **Улучшен docker-compose.yml** - добавлена политика перезапуска

### 📁 Новые файлы

```
wa_manager/
├── ecosystem.config.js       # Конфигурация PM2
├── PROCESS-MANAGEMENT.md     # Полная документация
└── AUTO-RESTART-SETUP.md     # Это руководство
```

### 🔧 Измененные файлы

- `package.json` - добавлен PM2, новые скрипты
- `Dockerfile` - использует PM2 вместо простого запуска
- `docker-compose.yml` - улучшена политика перезапуска

## Как это работает

### Многоуровневая защита

**Уровень 1: PM2 (внутри контейнера)**
- Перезапускает упавшие процессы за 4 секунды
- Ограничивает использование памяти
- Логирует все события

**Уровень 2: Docker**
- Перезапускает упавший контейнер
- Health check каждые 30 секунд
- Restart policy при сбоях

**Уровень 3: Приложение**
- Автоматически восстанавливает WhatsApp соединения
- Heartbeat мониторинг
- Graceful shutdown

## Тестирование

### Тест 1: Убить процесс

```bash
# Остановить API сервер
docker exec -it wa-manager pm2 stop api-server

# Подождать 4 секунды - процесс автоматически восстановится
docker exec -it wa-manager pm2 logs api-server
```

### Тест 2: Проверить статус

```bash
# Проверить количество перезапусков
docker exec -it wa-manager pm2 status

# Колонка ↺ показывает количество перезапусков
```

### Тест 3: Мониторинг в реальном времени

```bash
# Открыть интерактивный мониторинг
docker exec -it wa-manager pm2 monit
```

## Мониторинг

### Просмотр логов

```bash
# Все логи
docker-compose logs -f wa-manager

# PM2 логи внутри контейнера
docker exec -it wa-manager pm2 logs

# Логи из файлов
docker exec -it wa-manager tail -f logs/api-out.log
docker exec -it wa-manager tail -f logs/api-error.log
```

### Проверка здоровья

```bash
# Health check API
curl http://localhost:5001/health

# Статус Docker контейнера
docker inspect wa-manager | grep -A 10 Health
```

## Управление

### Перезапуск процессов

```bash
# Перезапустить все процессы
docker exec -it wa-manager pm2 restart all

# Перезапустить только API
docker exec -it wa-manager pm2 restart api-server

# Перезапустить только Next.js
docker exec -it wa-manager pm2 restart nextjs-server
```

### Остановка/запуск

```bash
# Остановить контейнер
docker-compose down

# Запустить контейнер
docker-compose up -d

# Перезапустить контейнер
docker-compose restart wa-manager
```

## Важные команды

```bash
# Статус процессов
docker exec -it wa-manager pm2 status

# Детальная информация
docker exec -it wa-manager pm2 describe api-server

# Мониторинг
docker exec -it wa-manager pm2 monit

# Логи
docker exec -it wa-manager pm2 logs

# Очистить логи
docker exec -it wa-manager pm2 flush
```

## Настройка (опционально)

### Изменить лимиты памяти

Отредактируйте `ecosystem.config.js`:

```javascript
{
  name: "api-server",
  max_memory_restart: "800M",  // Увеличить до 800MB
  // ...
}
```

### Изменить задержки перезапуска

```javascript
{
  restart_delay: 5000,  // 5 секунд вместо 4
  exp_backoff_restart_delay: 200,  // Увеличить экспоненту
  // ...
}
```

### Применить изменения

```bash
# Пересобрать контейнер
docker-compose up -d --build
```

## Проблемы и решения

### Процесс постоянно падает

```bash
# Проверить логи ошибок
docker exec -it wa-manager cat logs/api-error.log

# Проверить использование памяти
docker stats wa-manager

# Увеличить лимиты в ecosystem.config.js
```

### PM2 не запускается

```bash
# Проверить синтаксис конфигурации
docker exec -it wa-manager pm2 ecosystem ecosystem.config.js

# Попробовать ручной запуск
docker exec -it wa-manager node server/index.js
```

### Health check падает

```bash
# Проверить порты
docker exec -it wa-manager netstat -tulpn

# Проверить процессы
docker exec -it wa-manager ps aux

# Проверить PM2
docker exec -it wa-manager pm2 status
```

## Преимущества

✅ **Автоматическое восстановление** - процессы сами поднимаются при падении
✅ **Быстрое восстановление** - 4 секунды для процесса, 30-60 для контейнера
✅ **Защита от утечек памяти** - автоматический перезапуск при превышении лимита
✅ **Полное логирование** - все события записываются
✅ **Мониторинг** - встроенные инструменты PM2
✅ **Production-ready** - проверенное решение для высоконагруженных систем

## Что дальше?

1. ✅ Запустите систему: `docker-compose up -d --build`
2. ✅ Проверьте статус: `docker exec -it wa-manager pm2 status`
3. ✅ Протестируйте восстановление: остановите процесс и посмотрите как он восстановится
4. ✅ Настройте мониторинг: регулярно проверяйте логи и статус

Теперь ваша система полностью защищена от падений! 🚀

Подробная документация: [PROCESS-MANAGEMENT.md](./PROCESS-MANAGEMENT.md)
