# Исправление ошибки "table does not exist"

## 🔧 Быстрое решение

Выполните на сервере:

```bash
cd /var/www/wa_manager

# 1. Подтянуть изменения (миграции)
git pull origin master

# 2. Применить миграции внутри контейнера
docker-compose exec wa-manager npx prisma migrate deploy

# 3. Перезапустить контейнер
docker-compose restart wa-manager

# 4. Проверить логи
docker-compose logs -f wa-manager
```

## ✅ Должны увидеть:

```
Applying migration `20251111000000_init`

The following migration(s) have been applied:

migrations/
  └─ 20251111000000_init/
    └─ migration.sql

✅ All migrations have been successfully applied.
```

## 🎯 Если не работает - полная переустановка БД:

```bash
# ВНИМАНИЕ: Удалит все данные!

# Остановить контейнеры
docker-compose down -v

# Запустить заново
docker-compose up -d

# Проверить логи
docker-compose logs -f wa-manager
```

Теперь таблицы создадутся автоматически через CMD в Dockerfile.

---

## 📝 Что было исправлено:

✅ Добавлены миграции Prisma в репозиторий
✅ Dockerfile автоматически применяет миграции при старте
✅ Улучшена обработка ошибок БД в сервере

**После этих действий всё будет работать!** 🚀
