# Автоматическое восстановление клиентов 🔄

## ✅ Что теперь работает автоматически

После перезапуска сервера (`docker-compose down` → `docker-compose up`):

1. ✅ **Автоматическая очистка lock-файлов**
   - Удаляются блокировки Chromium
   - Нет ошибок "profile appears to be in use"

2. ✅ **Автоматическое восстановление подключенных аккаунтов**
   - Все аккаунты со статусом CONNECTED автоматически переподключаются
   - Используются сохранённые сессии из volume
   - **НЕ НУЖНО** вручную нажимать "Connect"!

3. ✅ **Сброс застрявших статусов**
   - Аккаунты в статусе CONNECTING/QR_READY/AUTHENTICATING → DISCONNECTED
   - Их нужно будет переподключить вручную

---

## 📊 Как это работает

### При запуске сервера:

```
🚀 WhatsApp API Server running on http://localhost:5001

🧹 Cleaning Chromium lock files...
  Removed: .wwebjs_auth/.../SingletonLock
✅ Lock files cleaned

🔄 Reset 0 stuck account(s) to DISCONNECTED

🔄 Auto-restoring 3 connected account(s)...

   Restoring: User Account 1 (cmhuhffkt0000ob08euwqksks)
   Restoring: User Account 2 (cmhuhogoh0004ob08wpequnls)
   Restoring: User Account 3 (cmhuixxxx0008ob08yyyyyyyyy)

✅ Auto-restore initiated for 3 account(s)

💡 Ready to accept connections

📊 Health check: http://localhost:5001/health
```

### Процесс восстановления (в фоне):

Для каждого аккаунта:
1. Инициализируется WhatsApp клиент
2. Загружается сохранённая сессия из `.wwebjs_auth/{accountId}`
3. Подключение к WhatsApp (10-30 секунд)
4. Статус остаётся CONNECTED
5. Аккаунт готов к отправке сообщений

---

## 🎯 Поведение после перезапуска

### Для пользователей с подключенными аккаунтами:

**Что происходит:**
- Обновите страницу в браузере (F5)
- Сначала увидите статус CONNECTED (из БД)
- Через 10-30 секунд клиент восстановится в фоне
- Можно сразу отправлять сообщения

**Никаких действий не требуется!** ✅

### Для новых аккаунтов или неподключенных:

**Что нужно сделать:**
- Статус: DISCONNECTED
- Нажмите "Connect"
- Отсканируйте QR код (если нет сессии)
- Подождите подключения

---

## 🔍 Частые вопросы

### Q: Сколько времени занимает восстановление?

**A:** 10-30 секунд на каждый аккаунт. Все аккаунты восстанавливаются **параллельно**.

Примеры:
- 1 аккаунт: ~15 секунд
- 3 аккаунта: ~20 секунд (параллельно)
- 10 аккаунтов: ~30 секунд (параллельно)

### Q: Что если сессия истекла или невалидна?

**A:** Автоматическое восстановление провалится:
- Статус изменится на FAILED
- В логах увидите ошибку
- Нужно будет вручную переподключить (новый QR код)

### Q: Можно ли отключить автовосстановление?

**A:** Да, если нужно (не рекомендуется):

В `server/index.js` закомментируйте:
```javascript
// Auto-restore connected clients
// await restoreConnectedClients();
```

Но тогда придётся переподключать вручную после каждого перезапуска.

### Q: Безопасно ли автоматическое переподключение для WhatsApp?

**A:** Да, потому что:
- Используются сохранённые официальные сессии
- Нет эмуляции или подозрительных действий
- Это как если бы вы просто перезапустили WhatsApp на телефоне
- Сессии валидны и авторизованы WhatsApp

### Q: Что происходит если перезапустить во время отправки сообщений?

**A:**
- Активные клиенты корректно закрываются (graceful shutdown)
- Сообщения в очереди могут потеряться
- После восстановления отправку нужно повторить
- Рекомендуется перезапускать в спокойное время

---

## 📝 Технические детали

### Какие аккаунты восстанавливаются:

✅ **Восстанавливаются автоматически:**
- Статус: CONNECTED

❌ **НЕ восстанавливаются (требуют ручного подключения):**
- Статус: DISCONNECTED
- Статус: FAILED
- Статус: CONNECTING (сбрасывается в DISCONNECTED)
- Статус: QR_READY (сбрасывается в DISCONNECTED)
- Статус: AUTHENTICATING (сбрасывается в DISCONNECTED)

### Где хранятся сессии:

```
Docker Volume: wa_manager_wa_sessions
├── session-{accountId1}/
│   ├── Default/
│   │   ├── Local Storage/
│   │   └── Session Storage/
│   └── ...
├── session-{accountId2}/
└── ...
```

### Порядок операций при старте:

1. Запуск Express сервера (порт 5001)
2. Запуск Next.js (порт 3000)
3. Применение миграций Prisma
4. **Очистка lock-файлов**
5. **Сброс застрявших статусов**
6. **Автовосстановление CONNECTED аккаунтов**
7. Готов к приёму запросов

---

## 🚀 Мониторинг восстановления

### Через логи:

```bash
docker-compose logs -f wa-manager
```

Ищите:
```
🔄 Auto-restoring X connected account(s)...
   Restoring: Account Name (id)
✅ Auto-restore initiated for X account(s)
```

Затем для каждого аккаунта:
```
QR Code received for {id}  # Если нет сессии
Client authenticated for {id}
Client ready for {id}
```

### Через Health Check:

```bash
curl http://localhost:5001/health | jq
```

```json
{
  "status": "ok",
  "activeClients": 3,
  "clients": [
    {
      "accountId": "cmhuhffkt0000ob08euwqksks",
      "status": "CONNECTED",
      "hasPhone": true
    }
  ]
}
```

### Через Web UI:

- Обновите страницу (F5)
- Статусы должны быть CONNECTED
- Индикаторы зелёные
- Можно отправлять сообщения

---

## ⚠️ Важные замечания

1. **Первый запуск после добавления аккаунта:**
   - Сессия еще не сохранена
   - Потребуется ручное подключение + QR код
   - После успешного подключения сессия сохранится
   - Следующий перезапуск - автоматическое восстановление

2. **Не удаляйте volumes:**
   ```bash
   # ❌ НЕ ДЕЛАЙТЕ ТАК (удалит сессии!)
   docker-compose down -v

   # ✅ ИСПОЛЬЗУЙТЕ ТАК
   docker-compose down
   docker-compose up -d
   ```

3. **Регулярные перезапуски:**
   - Автовосстановление работает каждый раз
   - Сессии валидны несколько месяцев
   - WhatsApp может инвалидировать сессии при подозрительной активности

4. **Множественные аккаунты:**
   - Все восстанавливаются параллельно
   - Не блокируют друг друга
   - Ограничение - ресурсы сервера (RAM/CPU)

---

## ✅ Преимущества автовосстановления

✅ Не нужно вручную переподключать после обновлений
✅ Меньше downtime для пользователей
✅ Автоматизация DevOps процессов
✅ Сессии используются повторно (экономия времени)
✅ Работает с `docker-compose restart` и `docker-compose down/up`

---

## 🎯 Итог

**После перезапуска сервера:**
1. Подождите 20-30 секунд
2. Обновите страницу в браузере
3. Всё работает автоматически!

**Никаких ручных действий не требуется!** 🚀
