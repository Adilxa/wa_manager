# Contract API - Документация

Система контрактов для массовой рассылки сообщений через WhatsApp с автоматическим управлением лимитами и отслеживанием результатов.

## Основные возможности

- ✅ Создание контрактов на массовую рассылку (например, 1000 сообщений)
- ✅ Автоматическое управление rate limiting и daily limits
- ✅ Отслеживание успешных (success) и неудачных (failure) отправок
- ✅ Получение списков номеров телефонов для success/failure
- ✅ Human-like поведение для избежания бана WhatsApp
- ✅ Пауза и возобновление обработки контракта
- ✅ Детальная статистика по каждому контракту

## Как это работает

1. **Создаете контракт** с массивом получателей (до 1000+ сообщений)
2. **Система автоматически** добавляет все сообщения в очередь
3. **Обработка с учетом лимитов:**
   - Rate limiting (20 сообщений/минуту)
   - Daily limits (500-1000 сообщений/день)
   - Автоматические перерывы после каждых 5 сообщений
   - Human-like задержки и typing indicators
4. **Отслеживание результатов** в реальном времени
5. **Получение финального отчета** с списками успешных и неудачных номеров

## API Endpoints

### 1. Создать контракт

**POST** `/api/contracts`

Создает новый контракт для массовой рассылки.

**Request Body:**
```json
{
  "accountId": "clxxxx",
  "name": "Рассылка акции 2024",
  "recipients": [
    {
      "phoneNumber": "79991234567",
      "message": "Привет! У нас акция..."
    },
    {
      "phoneNumber": "79997654321",
      "message": "Привет! У нас акция..."
    }
  ]
}
```

**Response:**
```json
{
  "id": "contract_xxx",
  "accountId": "clxxxx",
  "name": "Рассылка акции 2024",
  "totalCount": 2,
  "successCount": 0,
  "failureCount": 0,
  "pendingCount": 2,
  "status": "PENDING",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "recipients": [...]
}
```

### 2. Получить все контракты

**GET** `/api/contracts`

Параметры:
- `accountId` (optional) - фильтр по аккаунту
- `status` (optional) - фильтр по статусу (PENDING, IN_PROGRESS, PAUSED, COMPLETED, FAILED)

**Response:**
```json
[
  {
    "id": "contract_xxx",
    "name": "Рассылка акции 2024",
    "totalCount": 1000,
    "successCount": 750,
    "failureCount": 50,
    "pendingCount": 200,
    "status": "IN_PROGRESS",
    "createdAt": "2024-01-01T00:00:00.000Z",
    "account": {
      "id": "clxxxx",
      "name": "Main Account",
      "phoneNumber": "79990000000"
    }
  }
]
```

### 3. Получить детали контракта

**GET** `/api/contracts/:id`

**Response:**
```json
{
  "id": "contract_xxx",
  "name": "Рассылка акции 2024",
  "totalCount": 1000,
  "successCount": 750,
  "failureCount": 50,
  "pendingCount": 200,
  "status": "IN_PROGRESS",
  "account": {
    "id": "clxxxx",
    "name": "Main Account"
  },
  "recipients": [
    {
      "id": "rec_xxx",
      "phoneNumber": "79991234567",
      "message": "Привет! У нас акция...",
      "status": "SUCCESS",
      "sentAt": "2024-01-01T00:00:00.000Z"
    }
  ]
}
```

### 4. Запустить/Возобновить контракт

**POST** `/api/contracts/:id/start`

Запускает обработку контракта. Система автоматически:
- Проверяет лимиты (rate limit, daily limit)
- Добавляет задержки между сообщениями
- Делает перерывы после каждых 5 сообщений
- Обновляет статусы в реальном времени

**Response:**
```json
{
  "success": true,
  "message": "Contract processing started",
  "contractId": "contract_xxx",
  "pendingRecipients": 200
}
```

### 5. Приостановить контракт

**POST** `/api/contracts/:id/pause`

Приостанавливает обработку контракта. Можно возобновить позже.

**Response:**
```json
{
  "success": true,
  "message": "Contract paused"
}
```

### 6. Получить статистику контракта

**GET** `/api/contracts/:id/stats`

Получить детальную статистику с номерами телефонов.

**Response:**
```json
{
  "contractId": "contract_xxx",
  "name": "Рассылка акции 2024",
  "status": "COMPLETED",
  "total": 1000,
  "success": 950,
  "failed": 50,
  "pending": 0,
  "successRate": "95.00%",

  "successPhoneNumbers": [
    {
      "phoneNumber": "79991234567",
      "sentAt": "2024-01-01T00:00:00.000Z"
    },
    {
      "phoneNumber": "79997654321",
      "sentAt": "2024-01-01T00:01:00.000Z"
    }
  ],

  "failedPhoneNumbers": [
    {
      "phoneNumber": "79990000000",
      "errorMessage": "Invalid number",
      "attempts": 3
    }
  ],

  "pendingPhoneNumbers": [],

  "duration": "3600s",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "startedAt": "2024-01-01T00:00:00.000Z",
  "completedAt": "2024-01-01T01:00:00.000Z"
}
```

### 7. Удалить контракт

**DELETE** `/api/contracts/:id`

Удаляет контракт и все связанные данные.

**Response:**
```json
{
  "success": true
}
```

## Пример использования

### Шаг 1: Создать контракт
```bash
curl -X POST http://localhost:5001/api/contracts \
  -H "Content-Type: application/json" \
  -d '{
    "accountId": "clxxxx",
    "name": "Новогодняя рассылка 2024",
    "recipients": [
      {
        "phoneNumber": "79991234567",
        "message": "С Новым Годом! Специальная акция для вас..."
      },
      {
        "phoneNumber": "79997654321",
        "message": "С Новым Годом! Специальная акция для вас..."
      }
    ]
  }'
```

### Шаг 2: Запустить обработку
```bash
curl -X POST http://localhost:5001/api/contracts/contract_xxx/start
```

### Шаг 3: Отслеживать прогресс
```bash
# Получить общую информацию
curl http://localhost:5001/api/contracts/contract_xxx

# Получить детальную статистику
curl http://localhost:5001/api/contracts/contract_xxx/stats
```

### Шаг 4: При необходимости приостановить
```bash
curl -X POST http://localhost:5001/api/contracts/contract_xxx/pause
```

## Статусы контракта

- **PENDING** - Контракт создан, ожидает запуска
- **IN_PROGRESS** - Контракт в процессе обработки
- **PAUSED** - Контракт приостановлен (можно возобновить)
- **COMPLETED** - Все сообщения обработаны
- **FAILED** - Критическая ошибка обработки

## Статусы получателей

- **PENDING** - Ожидает отправки
- **QUEUED** - Добавлен в очередь
- **SENDING** - Отправляется сейчас
- **SUCCESS** - Успешно отправлено
- **FAILED** - Не удалось отправить

## Защита от бана WhatsApp

Система автоматически применяет следующие меры:

1. **Rate Limiting**: Максимум 20 сообщений в минуту
2. **Daily Limits**:
   - Новые аккаунты (< 7 дней): 500 сообщений/день
   - Старые аккаунты: 1000 сообщений/день
3. **Перерывы**: Автоматический отдых после каждых 5 сообщений (30-120 сек)
4. **Human-like поведение**:
   - Случайные задержки перед началом печати (0.5-2 сек)
   - Typing indicators
   - Эмуляция времени печати на основе длины сообщения
   - Случайные задержки между сообщениями (3-8 сек)

## Отключение лимитов

Если вы уверены в своем аккаунте и хотите отключить лимиты:

```bash
# Обновить аккаунт
curl -X PUT http://localhost:5001/api/accounts/clxxxx \
  -H "Content-Type: application/json" \
  -d '{"useLimits": false}'
```

**Внимание**: Отключение лимитов повышает риск бана!

## Мониторинг в реальном времени

Логи сервера показывают детальный прогресс:

```
📋 Processing contract Рассылка (contract_xxx): 1000 recipients pending
📤 Sending to 79991234567 (Contract: Рассылка)
✅ SUCCESS: 79991234567 (Contract: Рассылка)
⏱️  Waiting 5s before next message...
💤 Account clxxxx taking a break for 60 seconds after 5 messages
✨ Account clxxxx finished resting, resuming...
🎉 CONTRACT COMPLETED: Рассылка
   ✅ Success: 950
   ❌ Failed: 50
```

## Рекомендации

1. **Начинайте с малого**: Первый контракт на 10-20 сообщений для теста
2. **Используйте лимиты**: Оставляйте `useLimits: true` для новых аккаунтов
3. **Мониторьте**: Проверяйте статистику после каждого контракта
4. **Качество номеров**: Используйте валидные номера для снижения failures
5. **Время отправки**: Лучше отправлять в рабочие часы (10:00-20:00)

## Troubleshooting

**Q: Контракт застрял в IN_PROGRESS**
A: Проверьте что аккаунт подключен. Если нет - переподключите аккаунт, затем возобновите контракт через `/start`

**Q: Много FAILED сообщений**
A: Проверьте корректность номеров телефонов и статус аккаунта

**Q: Хочу ускорить отправку**
A: Установите `useLimits: false` для аккаунта (на свой риск!)

**Q: Как отменить контракт?**
A: Используйте `/pause` для остановки, затем `/api/contracts/:id` DELETE для удаления
