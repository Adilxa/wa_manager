# Поведение при перезапуске сервера

## 🔄 Что происходит при `docker-compose down` и `docker-compose up`

### Автоматические действия сервера:

1. **Очистка lock-файлов Chromium**
   - Удаляются все блокировки профилей
   - Предотвращает ошибки "profile appears to be in use"

2. **Сброс всех аккаунтов в DISCONNECTED**
   - ВСЕ аккаунты (независимо от статуса) переводятся в DISCONNECTED
   - Очищаются QR коды из базы данных
   - Клиенты WhatsApp НЕ восстанавливаются автоматически

### Почему клиенты не восстанавливаются автоматически?

✅ **Безопасность:**
- Автоматическое переподключение может вызвать бан от WhatsApp
- Множественные попытки подключения с разных IP могут быть заблокированы

✅ **Стабильность:**
- Сессии могут быть невалидными после длительного простоя
- Пользователь должен явно подтвердить переподключение

✅ **Контроль:**
- Администратор видит какие аккаунты активны
- Можно выборочно переподключить нужные аккаунты

---

## 👤 Что должен сделать пользователь после перезапуска

### Шаг 1: Проверить статус аккаунтов

Откройте веб-интерфейс:
- Все аккаунты будут в статусе **DISCONNECTED**
- Это нормально и ожидаемо!

### Шаг 2: Переподключить нужные аккаунты

Для каждого аккаунта:
1. Нажмите кнопку **"Connect"**
2. Если есть сохранённая сессия - подключится автоматически
3. Если нет сессии - отсканируйте QR код заново

### Шаг 3: Дождаться подключения

- Статус изменится: CONNECTING → QR_READY → AUTHENTICATING → CONNECTED
- Это занимает 10-30 секунд

---

## 🔍 Частые вопросы

### Q: Почему после перезапуска пишет "Client not initialized"?

**A:** Это нормально! После перезапуска контейнера все клиенты в памяти очищаются. Нужно:
1. Обновить страницу в браузере (F5)
2. Увидите статус DISCONNECTED
3. Нажать "Connect" для переподключения

### Q: Нужно ли сканировать QR код заново?

**A:** Нет, если сессия сохранена:
- Сессии хранятся в Docker volume `wa_sessions`
- При нажатии "Connect" используется сохранённая сессия
- QR код нужен только для новых аккаунтов или если сессия истекла

### Q: Я не хочу переподключать вручную каждый раз!

**A:** Есть два решения:

**Вариант 1: Не останавливайте контейнер**
```bash
# Вместо docker-compose down используйте restart
docker-compose restart wa-manager

# Это сохранит клиентов в памяти
```

**Вариант 2: Автовосстановление (не рекомендуется)**
- Можно включить автоматическое восстановление клиентов
- НО это может привести к бану от WhatsApp
- Не рекомендуется для production

### Q: Теряются ли сессии при перезапуске?

**A:** НЕТ! Сессии сохраняются:
- Локация: Docker volume `wa_sessions`
- Сохраняются между перезапусками
- Остаются даже после `docker-compose down -v` (если не удалить volume явно)

### Q: Что делать если сессия всё равно слетела?

**A:** Возможные причины:
1. WhatsApp отключил сессию из-за подозрительной активности
2. Слишком долгий перерыв между подключениями
3. Подключение с другого устройства

**Решение:**
- Просто отсканируйте QR код заново
- Сессия сохранится для следующего раза

---

## 🛠️ Технические детали

### Где хранятся данные:

```
Docker Volumes:
├── wa_sessions/          # WhatsApp сессии (LocalAuth)
│   ├── session-{id}/     # Сессия для каждого аккаунта
│   └── ...
├── wa_cache/             # Кэш браузеров
├── wa_logs/              # Логи (если включены)
└── postgres_data/        # База данных PostgreSQL
    └── whatsapp_accounts # Метаданные аккаунтов
```

### Что очищается при restart:

❌ **Очищается (в памяти):**
- Map клиентов WhatsApp
- Активные браузеры Puppeteer
- Текущие QR коды в памяти

✅ **Сохраняется (на диске):**
- Сессии WhatsApp (`.wwebjs_auth`)
- Данные в PostgreSQL
- История сообщений
- Метаданные аккаунтов

---

## 📊 Сравнение: down vs restart

| Действие | `docker-compose down` | `docker-compose restart` |
|----------|----------------------|--------------------------|
| Контейнер | ❌ Удаляется | ✅ Перезапускается |
| Клиенты в памяти | ❌ Теряются | ✅ Сохраняются* |
| Volumes | ✅ Сохраняются | ✅ Сохраняются |
| База данных | ✅ Сохраняется | ✅ Сохраняется |
| Сессии WhatsApp | ✅ Сохраняются | ✅ Сохраняются |
| Нужно переподключать | ✅ Да | ❌ Нет* |

\* *Зависит от graceful shutdown*

---

## 💡 Рекомендации

### Для обновления кода:
```bash
# ОБЯЗАТЕЛЬНО пересборка образа
docker-compose down
docker-compose build --no-cache wa-manager
docker-compose up -d
# Потребуется переподключение
```

### Для обычного перезапуска:
```bash
# Мягкий перезапуск
docker-compose restart wa-manager
# Клиенты ДОЛЖНЫ остаться активными
```

### Для обновления конфига:
```bash
# Применить изменения .env
docker-compose down
docker-compose up -d
# Потребуется переподключение
```

---

## ⚠️ Важно

1. **Всегда используйте volumes** для хранения сессий
2. **Не используйте `down -v`** без необходимости (удалит сессии!)
3. **После `down` ожидайте статус DISCONNECTED** - это нормально
4. **Переподключайте аккаунты вручную** - это безопаснее

---

**Это задуманное поведение для стабильности и безопасности системы!** ✅
